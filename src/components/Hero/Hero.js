import React from 'react';

const styles = {
	wrapper: {
		height: 300,
		backgroundColor: '#eee',
	},
};

function Hero() {
	return (
		<div style={styles.wrapper}>
			<div></div>
		</div>
	);
}

export default Hero;
